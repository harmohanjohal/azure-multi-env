ame: Hello Workflow

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say hello
        run: |
          echo "Hello from GitHub Actions!"
          echo "Current directory:"
          pwd
          echo "Files:"
          ls
